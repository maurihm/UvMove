spring.datasource.url=jdbc:db2://localhost:50000/UVMOVE
spring.datasource.username=db2inst1
spring.datasource.password=password_de_prueba
spring.datasource.driver-class-name=com.ibm.db2.jcc.DB2Driver

# Hibernate ddl-auto set to validate or update (since we use a script to init DB, let's just use none or validate, we'll use update for safety if script not run, but script is preferred)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.DB2Dialect

# Web port
server.port=8080

# CORS globally or locally - let's set it in controller or configuration
